const btn=document.getElementById('theme');btn?.addEventListener('click',()=>{document.body.classList.toggle('dark');localStorage.setItem('theme',document.body.classList.contains('dark')?'dark':'light')});if(localStorage.getItem('theme')==='dark')document.body.classList.add('dark');
const input=document.getElementById('search');let idx=[];fetch('/assets/search-index.json').then(r=>r.json()).then(j=>idx=j).catch(()=>{});input?.addEventListener('input',e=>{document.getElementById('results')?.remove();const q=e.target.value.trim().toLowerCase();if(!q)return;const hits=idx.filter(x=>(x.title+' '+x.tags.join(' ')+' '+x.text).toLowerCase().includes(q)).slice(0,8);if(!hits.length)return;const box=document.createElement('div');box.id='results';hits.forEach(h=>{const a=document.createElement('a');a.className='result';a.href=h.url;a.innerHTML=`<strong>${h.title}</strong><br><small>${h.tags.map(t=>'#'+t).join(' ')}</small>`;box.appendChild(a)});input.parentElement.appendChild(box)});
